#pragma once

#include <glad/glad.h>
#include <GLFW/glfw3.h>

#include "renderer.h"

#include <vector>

class Pass
{
  public:
    
  private:
    // Vector of draw commands
    // Vector of resources, like textures or render targets
};
